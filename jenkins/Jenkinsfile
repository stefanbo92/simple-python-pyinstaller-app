pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'rikorose/gcc-cmake'
                }
            }
            steps {
				sh 'cmake .. '
				sh 'make'
            }
        }
    }
}
